{
  "schema": 1,
  "name": "publicador",
  "description": "Drop a post spec, publicador queues, retries, schedules, and dispatches it to every platform",
  "input": "a spec.json path as the first argument: {caption, media, platforms, schedule_at?}",
  "output": "queued post id and per-platform publish results on stdout; --json for a machine envelope",
  "kind": "module",
  "tags": ["publishing", "scheduler", "queue", "retry", "instagram", "tiktok", "youtube"],
  "license": "MIT",
  "requires": ["python>=3.9"],
  "needs": ["edersonff/cookie-manager"],
  "entry": { "cli": "publicador" },
  "cost": { "model": "free" }
}
